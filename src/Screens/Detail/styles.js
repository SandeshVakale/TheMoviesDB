import {StyleSheet} from 'react-native';

const styles = StyleSheet.create({
  container: {
    flex: 1,
  },
  Icon: {
    position: 'absolute',
    top: 50,
    right: 20,
    height: 50,
    width: 50,
  },
  image: {
    width: '100%',
    height: 500,
  },
});
export default styles;
